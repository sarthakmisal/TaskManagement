9th Dec 2025
Please check following task and complete it in next 2 days once you done we will take Google meet for same.


Task: Task Management App with Teams
A mid-hard full-stack assignment testing UI skills, backend logic, security, and relational data handling.

Frontend (React) Requirements
Login & Registration pages.

Dashboard showing assigned tasks.

Create task form with:

Title

Description

Due date

Priority

Assign tasks to team members.

Filtering options: priority, due date, completed/pending.

Reusable components & clean UI design.

Use Redux Toolkit or Context API for state management.

Backend (Node.js + Express + MongoDB)
API Modules
Users

Teams (Mapping user → team)

Tasks (assigned_to, created_by)

Endpoints
POST /auth/register

POST /auth/login (JWT token)

GET/POST/PUT/DELETE /tasks

GET/POST /teams

Security Requirements
JWT access + refresh token mechanism.

Password hashing using bcrypt.

Role-based access (Team Leader vs Member).

Input validation using express-validator.

Add rate-limiting on auth routes (optional bonus).

Other Requirements
Complete Postman API documentation.

Use Mongoose Models + Relationships (populate).

Minimal UI task board to display tasks.